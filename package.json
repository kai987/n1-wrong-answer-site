{
  "name": "n1-wrong-answer-site",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check js/*.js && node --check js/ui/*.js && node --check js/data/*.js && node --check theme.js && node --check theme-init.js"
  }
}
